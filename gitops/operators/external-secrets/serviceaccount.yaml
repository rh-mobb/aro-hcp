# Created without workload-identity annotations. Bootstrap publishes client ID
# in openshift-gitops/aro-platform-metadata; eso-from-metadata stamps it here.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: external-secrets-sa
  namespace: external-secrets-operator
  annotations:
    argocd.argoproj.io/sync-wave: "1"
